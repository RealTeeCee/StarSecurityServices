@using Models
@inject List<Category> categoryList

@foreach (var item in categoryList)
{
    <a class="btn btn-link" asp-area="Client" asp-action="Detail" asp-controller="Category" asp-route-categorySlug="@item.Slug"> @item.Name</a>
}